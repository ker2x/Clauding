class Circle(Object):
    fn __init__(self, radius: Double):
        self.radius = radius

    fn area(self) -> Double:
        return 3.14159265 * self.radius * self.radius

fn main():
    let c = Circle(5.0)
    print(c.area())
